{
  "name": "policy-to-action-copilot",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "vercel-build": "npm --prefix frontend install && npm --prefix frontend run build",
    "lint": "npm --prefix frontend run lint",
    "preview": "npm --prefix frontend run preview",
    "evals": "node evals/run-evals.mjs",
    "test:sanitize": "node tests/sanitize-input.test.mjs",
    "test:guardrails": "node tests/guardrails.test.mjs"
  },
  "dependencies": {
    "@google/genai": "^1.32.0",
    "zod": "^4.1.11"
  },
  "devDependencies": {
    "@types/node": "^24.3.1",
    "typescript": "^5.9.2"
  }
}
